////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

ESPAÑOL:

CONVERTIDOR_PDF_A_TXT_EFIERRO
Esta es una herramienta robusta y ligera desarrollada en Python para la extraccion masiva de texto desde archivos PDF, optimizada con una interfaz de estilo Neon/Modo Oscuro.
Que hace: Extrae texto de PDF a TXT de forma rapida y ordenada.
Tecnologias: Python 3.14, PyPDF2, Tkinter.
Creditos: Creado por www.fierroduque.com.


Como funciona?
La aplicacion utiliza la libreria PyPDF2 para procesar los documentos de manera eficiente:

Carga: El usuario selecciona un archivo PDF local mediante una ventana emergente.

Procesamiento: El script recorre cada pagina, detecta los caracteres y mantiene el orden secuencial del documento original.

Visualizacion: Se muestra una previsualizacion inmediata en el monitor de consola integrado con estetica de terminal.

Exportacion: El sistema genera un archivo de texto plano (.txt) con codificacion UTF-8 para asegurar la compatibilidad con acentos y caracteres especiales.

Como hacerlo funcionar?
A. Para Usuarios Finales (No programadores)
Si no sabes de programacion y solo quieres usar la aplicacion en Windows, sigue esta ruta:

En la columna derecha de esta pagina, busca la seccion llamada Releases.

Haz clic en la version mas reciente (ej. v1.0).

En la parte inferior de esa pagina, en la seccion Assets, haz clic en el archivo CONVERTIDOR_PDF_A_TXT_EFIERRO.exe.

Una vez descargado, muevelo a la carpeta que desees y haz doble clic para abrirlo.
Nota: No requiere instalacion. Si Windows muestra una advertencia de seguridad, haz clic en Mas informacion y luego en Ejecutar de todas formas.

B. Para Desarrolladores (Codigo Fuente)
Si deseas modificar el codigo o ejecutarlo directamente desde Python:

Clonar el proyecto: git clone https://github.com/eduardofierroduque-sudo/CONVERTIDOR_PDF_A_TXT_EFIERRO.git

Entrar a la carpeta: cd CONVERTIDOR_PDF_A_TXT_EFIERRO

Instalar dependencias: python -m pip install PyPDF2

Ejecutar: python main.py

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

ENGLISH

CONVERTIDOR_PDF_A_TXT_EFIERRO
This is a robust and lightweight tool developed in Python for bulk text extraction from PDF files, optimized with a Neon/Dark Mode style interface.
What it does: Extracts text from PDF to TXT quickly and in order.
Technologies: Python 3.14, PyPDF2, Tkinter.
Credits: Created by www.fierroduque.com.



How it works?
The application uses the PyPDF2 library to process documents efficiently:

Upload: The user selects a local PDF file via a pop-up window.

Processing: The script goes through each page, detects characters, and maintains the original document's sequential order.

Visualization: An immediate preview is displayed on the built-in console monitor with a terminal aesthetic.

Export: The system generates a plain text file (.txt) with UTF-8 encoding to ensure compatibility with accents and special characters.

How to make it work?
A. For End Users (Non-programmers)
If you do not know about programming and just want to use the application on Windows, follow this path:

In the right column of this page, look for the section called Releases.

Click on the latest version (e.g., v1.0).

At the bottom of that page, in the Assets section, click on the file CONVERTIDOR_PDF_A_TXT_EFIERRO.exe.

Once downloaded, move it to your desired folder and double-click to open it.
Note: No installation required. If Windows shows a security warning, click on More Info and then Run anyway.

B. For Developers (Source Code)
If you wish to modify the code or run it directly from Python:

Clone the project: git clone https://github.com/eduardofierroduque-sudo/CONVERTIDOR_PDF_A_TXT_EFIERRO.git

Enter the folder: cd CONVERTIDOR_PDF_A_TXT_EFIERRO

Install dependencies: python -m pip install PyPDF2

Run: python main.py
